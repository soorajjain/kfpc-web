    import React from "react";
    import ErrorPage from "./error";

    // This component can be used as the catch-all route for routing mismatches
    const NotFound = () => <ErrorPage />;

    export default NotFound;
